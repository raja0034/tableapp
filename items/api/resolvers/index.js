const Query = require("./query");
const Mutation = require("./mutation");
const Item = require("./items");
const User = require("./users");
const resolvers = { Query, Mutation, Item, User };

module.exports = resolvers;
